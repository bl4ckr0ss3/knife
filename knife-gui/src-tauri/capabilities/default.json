{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window: core defaults plus the file-open dialog.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
